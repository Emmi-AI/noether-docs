noether.core.configs
====================

.. py:module:: noether.core.configs


Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/noether/core/configs/hyperparameters/index


Classes
-------

.. autoapisummary::

   noether.core.configs.Hyperparameters


Package Contents
----------------

.. py:class:: Hyperparameters

   Utility class to store and log hyperparameters configurations from a Pydantic model.


   .. py:method:: save_resolved(stage_hyperparameters, out_file_uri)
      :staticmethod:


      Save the resolved config schema hyperparameters to the output file.


      :param stage_hyperparameters: Hyperparameters to save in a Pydantic object.
      :param out_file_uri: Path to the output file.

      :returns: None



   .. py:method:: load_resolved(file_uri)
      :staticmethod:


      Inverse of :meth:`save_resolved` — load a previously-saved resolved config.

      Reads the YAML with ``yaml.full_load`` (preserves ``!!python/tuple``
      tags emitted for tuple-typed fields like dataset statistics), resolves
      the ``config_schema_kind`` marker written by ``save_resolved`` to the
      concrete :class:`ConfigSchema` subclass, and validates the rest of
      the dict through Pydantic.

      :param file_uri: Path to the YAML file produced by ``save_resolved``
                       (typically ``<run_dir>/hp_resolved.yaml``).

      :returns: The validated config schema instance.

      :raises FileNotFoundError: if ``file_uri`` doesn't exist.
      :raises pydantic.ValidationError: if the file is missing required fields
          or the config schema's validators reject it.



   .. py:method:: log(stage_hyperparameters)
      :staticmethod:


      Logs the stage hyperparameters in YAML format without trailing newlines.

      :param stage_hyperparameters: The hyperparameters configuration to log.

      :returns: None



